#include <bits/stdc++.h>

#ifndef ONLINE_JUDGE
#define kushinada freopen(getenv("MY_PATH"), "r", stdin);
#else
#define kushinada
#endif

using i64 = long long;
using P = std::pair<int, int>;
using TP = std::tuple<int, int, int>;

int main() {
  std::cin.tie(NULL)->sync_with_stdio(false);
  kushinada;
  std::string s;
  std::cin >> s;
  std::vector<int> cnt(3);
  for (auto c : s) {
    cnt[c - 'a']++;
  }

  int x = *std::ranges::min_element(cnt);
  cnt[0] -= x;
  cnt[1] -= x;
  cnt[2] -= x;

  if (cnt[0] + cnt[1] + cnt[2] < 3 && cnt[0] < 2 && cnt[1] < 2 && cnt[2] < 2) {
    std::println("YES");
  } else {
    std::println("NO");
  }
}