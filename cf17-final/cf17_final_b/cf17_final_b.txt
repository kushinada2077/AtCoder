#include <bits/stdc++.h>

#ifndef ONLINE_JUDGE
#define kushinada freopen(getenv("MY_PATH"), "r", stdin);
#else
#define kushinada
#endif

using i64 = long long;
using P = std::pair<int, int>;
using TP = std::tuple<int, int, int>;

int main() {
  std::cin.tie(NULL)->sync_with_stdio(false);
  kushinada;
  std::string s;
  std::cin >> s;
  std::vector<int> cnt(3);
  for (auto c : s) {
    cnt[c - 'a']++;
  }

  int min_v = *std::ranges::min_element(cnt);
  int max_v = *std::ranges::max_element(cnt);

  if (max_v - min_v < 2) {
    std::println("YES");
  } else {
    std::println("NO");
  }
}