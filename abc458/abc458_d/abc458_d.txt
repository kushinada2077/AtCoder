#include <bits/stdc++.h>

#ifndef ONLINE_JUDGE
#define kushinada freopen(getenv("MY_PATH"), "r", stdin);
#else
#define kushinada
#endif

using i64 = long long;
using P = std::pair<int, char>;
using TP = std::tuple<int, int, int>;

int main() {
  std::cin.tie(NULL)->sync_with_stdio(false);
  kushinada;
  int X, Q;
  std::cin >> X >> Q;
  std::multiset<int> S;
  S.insert(X);
  auto it = S.begin();
  for (int i = 0; i < Q; ++i) {
    int A, B;
    std::cin >> A >> B;
    S.insert(A);
    S.insert(B);
    if (std::min(A, B) > *it) {
      it = std::next(it);
    } else if (std::max(A, B) < *it) {
      it = std::prev(it);
    }

    std::println("{}", *it);
  }
}